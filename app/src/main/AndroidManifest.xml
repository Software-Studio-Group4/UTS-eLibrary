<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.utselibrary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminDashboard"></activity>
        <activity android:name=".AdminLogin" />
        <activity android:name=".BookDetails" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".Dashboard" />
        <activity android:name=".UserLogin" />
        <activity android:name=".UserRegister" />
        <activity android:name=".UserRegister$RegisterFinishPge" />
        <activity android:name=".UserRegister$RegisterDetailsPge" />
        <activity android:name=".UserRegister$RegisterPassPge" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>